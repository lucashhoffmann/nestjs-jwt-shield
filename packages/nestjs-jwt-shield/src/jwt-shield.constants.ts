export const JWT_SHIELD_OPTIONS = Symbol('JWT_SHIELD_OPTIONS');

export const JWT_SHIELD_PUBLIC_METADATA = 'jwt-shield:public';

export const JWT_SHIELD_SCOPES_METADATA = 'jwt-shield:scopes';

export const JWT_SHIELD_IMPERSONATION_POLICY_METADATA =
  'jwt-shield:impersonation-policy';
